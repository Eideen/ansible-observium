---
- name: install epel repository
  yum:
   name: epel-release
   state: present


- name: install prerequisites
  yum:
   name: "{{ item }}"
   state: present
  with_items: "{{ observium_packages }}"
